let s;
function createSquares(){
  s = new Clickable(200,200);
  s.resize(50,50)
  s.color = " red"
  s.text = "W"
  s.cornerRadius = 1
  //try text color
  //try stroke
  //try w
  
}//end CreateSquare
